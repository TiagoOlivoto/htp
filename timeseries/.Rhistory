# Ìndices de vegetação
library(pliman)
set_wd_here()
indices <- pliman_indexes()
indices <- pliman_indexes_eq()
View(indices)
shapefile <- shapefile_input("shapefile.rds")
# Ìndices de vegetação
library(pliman)
set_wd_here()
shapefile <- shapefile_input("shapefile.rds")
set_wd_here("timeseries")
shapefile <- shapefile_input("shapefile.rds")
shapes <- list.files(pattern = "2024")
shapes
rasters <- mosaic_input(shapes)
shapes
